version: 2
    
jobs:   
  build:
    working_directory: ~/workspace        
    docker:
      - image: docker:18.03-git
    steps:
      - checkout
      - setup_remote_docker
      - run:
          command: |
            docker build -t gunosy/java8-mecab-ipadic-neologd .
      - deploy:
          command: |
            if [ "${CIRCLE_BRANCH}" == "master" ]; then
              export TAG=`date +%Y.%m.%d`
              docker login -u "$DOCKER_HUB_USER" -p "$DOCKER_HUB_PASSWORD"
              docker tag gunosy/java8-mecab-ipadic-neologd gunosy/java8-mecab-ipadic-neologd:"$TAG"
              docker push gunosy/java8-mecab-ipadic-neologd:"$TAG"
              docker tag gunosy/java8-mecab-ipadic-neologd gunosy/java8-mecab-ipadic-neologd:latest
              docker push gunosy/java8-mecab-ipadic-neologd:latest
            fi
            
workflows:
  version: 2
  workflow:
    triggers:
      - schedule:
          cron: "0 0 * * 2,5" 
          filters:
            branches:
              only:
                - master
    jobs:
      - build

